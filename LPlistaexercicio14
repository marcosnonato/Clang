#include <stdio.h>

int main()
{
char frase[100];
	printf("\nEscreva uma frase: ");
	scanf(" %[^\n]s", frase);
	
	printf("%s\n", frase);

	
	int i, tamanho;
	for (i=0; frase[i] !='\0'; i++)
		tamanho = tamanho +1;
		

	
	for (i = (tamanho - 1);i >=0; i--)
    printf("%c", frase[i]); 		
		
	
	
return 0;
}
